// Marketing layout — wraps public landing + online booking flow
// Design shell added in Phase 2
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
